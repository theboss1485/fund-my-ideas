const Comment = (props) => {

    return (

        <section className="custom-tab has-text-centered">
            <div className="card">
                <div>
                    <div>

                    </div>
                </div>
            </div> 
            <p>{props.commentText}</p>

        </section>
    )
}